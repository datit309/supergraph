# Tech stack
- Bash is executable implementation language; Markdown/JSON/YAML hold skills, manifests, docs, hooks, and CI.
- No package-manager project manifest at repository root.
- Plugin version must remain synchronized across platform manifests.
- Codebase Memory MCP `>= 0.10.8,<0.11.0` is required; Serena is optional LSP intelligence.
