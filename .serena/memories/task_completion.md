# Task completion
- Run all `plugins/supergraph/tests/test-*.sh` scripts.
- Run Bash syntax checks on changed shell files.
- Reindex Codebase Memory after edits and inspect changed graph evidence.
- Run `/supergraph:fix`, optional `/supergraph:integration`, `/supergraph:verify`, then independent `/supergraph:review`.
- Never claim completion without fresh command output.