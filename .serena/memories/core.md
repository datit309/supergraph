# Supergraph
- Multi-platform AI coding-workflow plugin under `plugins/supergraph/`.
- Platform adapters: Claude marketplace, Codex manifest, Antigravity symlink installer, OpenCode skill links.
- Mandatory workflow and graph invariants live in `AGENTS.md` and plugin skills.
- Read `mem:tech_stack` for runtime/tooling, `mem:conventions` for implementation rules, and `mem:task_completion` before sign-off.